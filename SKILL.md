---
name: business-flow-writer
description: 業務改善支援で使うBPMN準拠の業務フロー図をExcelで作成するスキル。「業務フロー書いて」「フロー図作って」「BPMNで整理して」「この業務をフロー化して」などの依頼で発動。ヒアリング内容（業務の流れ・登場人物・成果物・制約事項）を中間定義表で確認してから、オートシェイプで描画したxlsxを生成する。生成後の手作業ブラッシュアップを前提とする。
---

# business-flow-writer

自治体業務の業務フロー図を、デジタル庁「地方公共団体の基幹業務システムの標準仕様における業務フロー」（J-LIS 機能要件表記方法利用ガイド準拠）の**分析レベル**で作成する。

成果物は Excel (.xlsx)。図はすべてオートシェイプで描画するため、利用者が後から図形のドラッグ・テキスト編集・線の付け替えで自由に修正できる。**8割を自動生成し、最後の見栄えは手作業で仕上げる**運用。

> **前提環境**: Windows + Microsoft Excel（COM経由で描画）+ PowerShell 7。PDFの自己検証（Step 5）には Poppler（`pdftoppm`）を任意で使う。詳細は [README.md](README.md) を参照。

記法・JSONスキーマ・レイアウト規約の詳細は [references/notation.md](references/notation.md) を必ず読むこと。

## ワークフロー

### Step 1: ヒアリング

以下を聞き取る。すでに資料（議事録・メモ・既存資料）があれば読み込んで補完し、不足分だけ質問する。一度に大量の質問を投げず、重要な順に聞く。

1. **作業番号の要否**（**最初に必ず確認する**。001, 002... の番号を振るかどうか。手作業で図を修正することが多い想定なら、番号なし（振り直しの手間が出る）を推奨）
2. **事務名**（フローのタイトル。「証明書の交付」のような事務単位）
3. **登場人物**（プールとレーン。例: 住民 / 担当課[窓口・バックヤード] / 委託事業者）
4. **開始のきっかけと終了状態**
5. **作業の流れ**（時系列。「何を使って何をする」の粒度。「ファイルを開く」「コピペする」まで落とさない）
6. **分岐・例外処理**（判断ポイントと Yes/No それぞれの行き先）
7. **成果物・書類**（申請書・証明書・通知など）
8. **使用システム**（基幹システム・外部サービスなど）
9. **制約事項・注記**（事務詳細。本人確認書類の種類、郵送対応の有無など）

### Step 2: 中間定義の提示（必須・スキップ禁止）

いきなり図を作らず、**作業一覧表を Markdown で提示して利用者の確認を取る**。図になってから直すより表で直す方が速いため。No列は作業番号を入れる場合のみ使う（Step 1 で番号なしと決めた場合は列ごと省略する）。

```markdown
| No | レーン | 作業 | 種別 | 次へ | 分岐条件 | 関連（書類・システム・注記） |
|----|--------|------|------|------|----------|------------------------------|
| 001 | 窓口 | 受付 | 人 | 002 | - | 申請書（住民から） |
| 005 | バックヤード | 対象者照会 | 人 | GW1 | - | 基幹システム |
| GW1 | バックヤード | 抑止対象者か | 分岐 | Yes→終了(例外) / No→006 | | 例外注記: 発行を抑止 |
```

プール・レーン構成もあわせて提示する。**承認を得てから Step 3 へ進む。**

### Step 3: フロー定義JSONの作成

承認された作業一覧を references/notation.md のスキーマに従って flow.json に変換し、出力フォルダに保存する。列（col）・行（row）の割り当て規約も notation.md に従う。

出力先の決め方:
- 案件フォルダで作業中なら、その成果物フォルダ（例: `<案件フォルダ>/deliverables/`）
- 指定がなければ作業フォルダ配下の `output/YYYYMMDD_<事務名>/`

### Step 4: Excel生成

スキルディレクトリからの相対パスでスクリプトを実行する。

```powershell
& "<スキルディレクトリ>/scripts/generate-flow.ps1" `
    -Definition <flow.json のパス> `
    -Output <出力.xlsx のパス> `
    -ExportPdf
```

### Step 5: 自己検証（必須）

生成した PDF を PNG 化して自分の目で確認する。`pdftoppm`（Poppler）が PATH 上にある前提。無い場合は Excel/PDF を直接開いて確認する。

```powershell
pdftoppm -png -r 150 <出力.pdf> <出力フォルダ>/preview
```

PNG を Read して以下をチェックし、問題があれば flow.json を直して再生成する:
- ノードの重なり・ラベルの欠け（列・行の割り当てを見直す）
- 注記や脚注の行切れ
- コネクタが意図と違う辺に付いている（flows に fromSite / toSite を明示して直す）

### Step 6: 引き渡し

利用者に以下を伝える:
- xlsx のパス（＋検証用PDF/PNGのパス）
- 図はすべてオートシェイプなので、図形のドラッグ・テキスト編集・コネクタの付け替えで自由に修正できること
- 修正した結果を次回に活かしたい場合は flow.json も一緒に保管しておくと再生成できること

## 注意

- 中間定義の確認を飛ばさない（Step 2 は必須）
- 書類・メールはデータオブジェクト（線でつながず、関連する流れの近くに置く）で表す。人/システムのマーカー記号は使わない
- 複雑な差し戻しループ・線の交差回避は自動化しない。「仮配置＋手修正前提」と明示して引き渡す
- このスキルは完全ローカルで動く（外部送信なし）。機密資料をヒアリング材料にする場合も、ファイルはローカルでのみ扱う
